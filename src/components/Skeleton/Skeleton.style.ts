import { styled } from "styled-components";

export const SkeletonStyle = styled.div`
  width: 100%;
  height: 20px;
  color: transparent;
  animation: ioAouA 2s ease infinite;
  pointer-events: none;
`;
